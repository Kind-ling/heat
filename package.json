{
  "name": "@kindling/heat",
  "version": "0.1.0",
  "description": "Heat — reputation-weighted routing oracle for the agent economy. /heat/score, /heat/route, /heat/trust.",
  "author": "Permanent Upper Class",
  "license": "MIT",
  "type": "module",
  "main": "./dist/api/server.js",
  "scripts": {
    "start": "node dist/api/server.js",
    "dev": "tsx src/api/server.ts",
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.1.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.0.0"
  }
}
